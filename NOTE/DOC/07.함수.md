# 함수
## 종류
- 윈도우 함수
- 기타 함수

### 숫자함수
→ CEIL (인자)       : 인자보다 크거나 같은 정수 중 제일 작은 수 반환
  [CEIL : '천장']   ex) CEIL (12.34) : 13 (결과값-정수)

→ FLOOR             : 인자보다 작거나 같은 정수 중 제일 큰 수 반환
  [FLOOR : '바닥']   ex) FLOOR (12.34) : 13 (결과값-정수)

→ ROUND (인자, 자리수)  : 인자를 지정한 자리수에서 반올림한 값을 반환
    * 자리수            : 소수점 아래 첫째 자리 0
                          소수점 아래 둘째 자리 1
                          소수점 아래 N번째 자리 N-1
                          정수 부분 N번째 자리  -N



                        

### 문자함수
- UPPER (인자)      : 인자(영문자)를 대문자로 변환
- LOWER (인자)      : 인자(영문자)를 소문자로 변환
- INITCAP(인자)     : 인자(영문자)를 단어를 기준으로 첫글자를 대문자로 변환

- LENGTH(인자)      : 인자의 글자 수를 반환하는 함수
- LENGTHB(인자1, 인자2) : 인자1과 인자2를 연결하는 함수

- CONCAT(인자1, 인자2) : 인자1과 인자2 문자열을 연결하는 함수
    ||              : 2개 이상의 문자열을 연결하는 기호

- SUBSTR (문자열, 시작번호, 글자수)
    → 문자열에서 시작번호부터 지정한 글자 수만큼의 문자열을 추출하여 반환

- SUBSTRB (문자열, 시작번호, 바이트수)
    → 문자열에서 시작번호부터 지정한 바이트 수만큼의 문자열을 추출하여 반환

- INSTR (문자열 문자 시작번호 순서)
    → 문자열 안에서 지정한 시작번호부터 문자를 찾아서
      지정한 순서에서 있는 문자의 위치를 반환하는 함수

- LPAD (문자열, 칸, 문자)   : 지정한 칸에서 왼쪽 빈공간을 특정문자로 채움
- RPAD (문자열, 칸, 문자)   : 지정한 칸에서 오른쪽 빈공간을 특정문자로 채움



### 날짜함수
- SYSDATE
    → 현재 날짜와 시간을 DATE 타입의 데이터로 반환

- SYSTIMESTAMP
    → 현재 날짜와 시간을 TIMESTAMP 타입의 데이터로 반환

- ADD_MONTHS (날짜, 개월 수)
    → 해당 날짜로부터 개월 수를 더한 날짜를 반환

- MONTHS_BETWEEN (날짜1, 날짜2)
    → 날짜 1 부터 날짜 2사이의 개월수를 반환

- NEXT DAT (날짜 , 요일번호)
    → 지정한 날짜 이후에 나오는 요일번호에 해당하는 날짜를 반환
    * 요일 번호 : 일요일 (1) ~ 토요일 (7)

- LAST DAY (날짜)
    → 지정한 날짜와 같은 달의 마지막 날짜를 반환 (월말)

- TRUNC (날짜, 날짜형식)
    → 날짜형식 단위로 데이터를 절삭하여 반환
    EX) TRUNC (날짜, 'MM')
        → 월 단위로 날짜 데이터를 절삭하여 같은달의 첫번째 날짜를 구하는 함수



### 변환함수
- TO_CHAR (인자, 출력포맷)  : 인자(날짜, 숫자)를 문자로 변환하는 함수

- 날짜 데이터 포맷
    CC          :       세기
    YYYY        :       연도 (4자리)
    YY          :       연도 (2자리)
    MM          :       월 (2자리)
    MON         :       월 (언어별 월 이름의 약자 - JAN / 1월)
    MONTH       :       월 (언어별 월 이름 - January / 1월)
    DD          :       일 (2자리)
    DDD         :       1년 중 몇일 (3자리) [1~366]
    DY          :       요일 (언어별 요일 이름의 약자)
    DAY         :       요일 (언어별 요일 이름)
    W           :       1년 중 몇번째 주 [1~53]
    HH24        :       24시 기준 시간
    HH          :       12시 기준 시간
    MI          :       분
    SS          :       초
    AM, PM      :       오전, 오후

- 숫자 데이터 포맷
    9           :         숫자 한자리 (값이 없으면 채우지 않음)
    0           :         숫자 한자리 (값이 없으면 0으로 채움)
    $           :         달러 표시
    L           :         지역 화폐 단위 기호
    .           :         소수점
    ,           :         천 단위 구분 기호


- TO_DATE ()    : 문자 데이터를 날짜 데이터로 변환
    * TO_DATE (문자, '변환포맷')
    ex) TO_DATE ('2025/11/24', 'YYYY/MM/DD')
        TO_DATE ('2025/01/01', 'YYYY/MM/DD')

- TO_NUMBER     : 문자 데이터를 숫자 데이터로 변환
    * TO_NUMBER (문자, '출력포맷')


### NULL 관련 함수
- NVL (인자1, 인자2)
    → 인자1이 NULL 이면, 인자2를 반환하는 함수

- NVL2 (인자1, 인자2, 인자3)
    → 인자1이 NULL 이면 인자2, 인자2도 NULL이면 인자3을 반환하는 함수

- COALESCE (인자1, 인자2, ...)
    → 인자들 중 NULL이 아닌 첫번째 값을 반환
    ex) 인자1 : NULL, 인자2 : NULL, 인자3 : (값)
        출력 : 인자3

- LNNVL (조건식) -- 반전술식 ㅇㅇ
    → 조건식의 결과를 TRUE FALSE 를 뒤집어서 반환
    1) TRUE         → FALSE
    2) NULL, FALSE  → TRUE

- NULLIF (인자1, 인자2)
    → 인자1과 인자2가 같으면 NULL
      인자1과 인자2가 다르면 인자1을 반환



### 그룹함수
- SUM(인자)     : 합계

- AVG(인자)     : 평균

- COUNT(인자)   : 개수

- MAX(인자)     : 최댓값
                * MAX(날짜)     : 가장 최근의 날짜
                * MAX(숫자)     : 가장 큰 숫자

- MIN(인자)     : 최솟값
                * MIN(날짜)     : 가장 과거의 날짜
                * MIN(숫자)     : 가장 작은 숫자

- STDDEV(인자)  : 표준편차

- VARIANCE(인자): 분산




### 그룹관련 함수
- ROLLUP (인자1, 인자2 ... )
    → 그룹 기준으로 집계한 정보를 레벨별로 출력

- CUBE (인자1, 인자2 ... )
    → 그룹 기준으로 집계한 정보를 가능한 모든 조합별로 출력

- GROUPING SETS ((인자 1, 인자2), (인자 3, 인자4) ... )
    → 그룹 컬럼이 여러개일때, 집계한 정보를 컬럼별로 출력

- GROUPING ()
    → 그룹화된 여부를 출력
    * 그룹화 O : 0
    * 그룹화 X : 1

- LISTAGG (나열할 컬럼, [구분자])
    WITHIN GROUP (ORDER BY 정렬기준 컬럼)
        → 그룹화된 데이터를 하나의 열에 목록으로 나열하여 출력

- PIVOT
    → 그룹화된 행 데이터를 열 데이터로 전환하여 출력
    * (
        SELECT 컬럼1, 컬럼2, 컬럼3
        FROM 테이블명
      )
      PIVOT (
        그룹함수 (컬럼명)
        FOR 피벗할 컬럼 IN (값1, 값2, 값3 ...)
      )

- UNPIVOT
    → → 그룹화된 열 데이터를 행 데이터로 전환하여 출력
    * UNPIVOT (
        기준컬럼
        FOR 피벗할 컬럼 IN (열1, 열2, 열3 ...)
    )

### 윈도우 함수
  → 행과 행 간의 관계를 정의하는 함수
    
  * 문법 구조
  '''
    SELECT 윈도우함수 (인수)
      OVER (
        [PARTITION BY 컬럼1, 컬럼2 ...]
        [ORDER BY 컬럼1 [ASC | DESC], 컬럼2 [ASC | DESC], ...]
        [WINDOWING 절]
      )
    FROM 테이블;
  '''

  * 윈도우 함수 분류
  - 순위 함수       : RANK, DENSE_RANK, ROW_NUMBER
  - 집계 함수       : SUM, AVG, COUNT, MAX, MIN
  - 행 순서 함수    : FIRST_VALUE, LAST_VALUE, LAG, LEAD
  - 비율 함수       : CUME_DIST, PERCENT_RANK, NTILE, RATTION_TO_REPORT




### 기타 함수
- GREATEST (인자1, 인자2, ...)
    → 인자들 중 가장 큰 값을 반환

- LEAST (인자1, 인자2, ...)
    → 인자들 중 가장 작은 값을 반환

- DECODE (인자1, 조건1, 인자2, 조건2, ...)
    → 인자의 값이 조건과 일치할 때, 그 뒤의 지정한 결과를 출력




### CASE 문
    → 특정 조건에 따라 반환할 데이터를 설정하는 조건문

    CASE
      WHEN 조건1 THEN 결과1     -- 조건1 만족 시, 결과1을 반환
      WHEN 조건2 THEN 결과2
      WHEN 조건3 THEN 결과3
      ...
      ELSE  결과 A              -- 위 모든 조건을 만족하지 않을때
    END                                        결과 A를 반환한다